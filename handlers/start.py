from aiogram import Router
from aiogram.filters import CommandStart
from aiogram.types import Message, InlineKeyboardMarkup, InlineKeyboardButton

# Создаём роутер для обработчиков
router = Router()


@router.message(CommandStart())
async def cmd_start(message: Message):
    """Обработчик команды /start — первое приветствие"""

    # Создаём кнопку "Начать расследование"
    keyboard = InlineKeyboardMarkup(
        inline_keyboard=[
            [
                InlineKeyboardButton(
                    text="🕵 Начать расследование",
                    callback_data="start_quiz",
                )
            ]
        ]
    )

    # Текст приветствия
    text = (
        "👋 Привет, друг!\n\n"
        "<b>Твой личный детектив по итогам года готов к расследованию!</b>\n\n"
        "Итоги 2025 года подводят все: кто-то поднялся в доходах, а кто-то до сих пор топчется на месте." 
        "Но не даёт покоя и волнует один главный вопрос: <b>что конкретно мешает лично тебе сделать результат х10?</b>\n\n"
        "Ответь на несколько вопросов — и я найду твоё узкое место всего за 2 минуты. Готов?"
    )



    
    # Отправляем сообщение с кнопкой
    await message.answer(text, reply_markup=keyboard, parse_mode='HTML')